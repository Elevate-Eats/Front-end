import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const PilihManager = () => {
  return (
    <View>
      <Text>PilihManager</Text>
    </View>
  )
}

export default PilihManager

const styles = StyleSheet.create({})