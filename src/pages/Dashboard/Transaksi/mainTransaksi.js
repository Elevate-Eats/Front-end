import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const MainTransaksi = () => {
  return (
    <View>
      <Text>MainTransaksi</Text>
    </View>
  );
};

export default MainTransaksi;

const styles = StyleSheet.create({});
